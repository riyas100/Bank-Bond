<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View bank.bond.type View Tree -->
    <record id="view_bank_bond_type_tree" model="ir.ui.view">
        <field name="name">view.bank.bond.type.tree</field>
        <field name="model">bank.bond.type</field>
        <field name="arch" type="xml">
            <list>
                <field name="name" />
            </list>
        </field>
    </record>

    <!-- View bank.bond.type form -->
    <record id="view_bank_bond_type_form" model="ir.ui.view">
        <field name="name">view.bank.bond.type.form</field>
        <field name="model">bank.bond.type</field>
        <field name="arch" type="xml">
            <form string="Bank Bond Type">
                <sheet>
                    <div class="oe_title">
                        <label for="name" />
                        <h1>
                            <field name="name" placeholder="Name..." />
                        </h1>
                    </div>
                    <notebook>
                        <page string="Notes">
                            <field name="notes" nolabel="1" />
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- View bank.bond.type search -->
    <record id="view_bank_bond_type_search" model="ir.ui.view">
        <field name="name">view.bank.bond.type.search</field>
        <field name="model">bank.bond.type</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}" />
                </group>
            </search>
        </field>
    </record>

    <!-- Action bank.bond.type -->
    <record id="action_bank_bond_type" model="ir.actions.act_window">
        <field name="name">Bank Bond Type</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">bank.bond.type</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Bank Bond Type.
            </p>
        </field>
    </record>
    
</odoo>
